$(document).ready(function () {
    $("[data-fancybox]").fancybox({
        closeBtn: true,
        overlay: {
            closeClick: false,
            css: { 'cursor': 'default' }
        }
    });
});